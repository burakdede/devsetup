# System essentials
build-essential
bzip2
ca-certificates
curl
diffutils
file
gettext-base
gnupg
less
openssl
p7zip-full
patch
pkg-config
python3
sqlite3

# python3 is the system interpreter, needed by system tooling and by this
# repo's own test suite. python3-pip and python3-venv are deliberately absent:
# mise owns the interpreter you develop against and uv owns Python CLI tools,
# so system pip would be a third, unused way to install packages.
tar
unzip
wget
xclip
xz-utils
zip
zstd

# CLI tools that macOS gets from the Brewfile; keep the two in step.
cmake
htop
moreutils
nmap

# Clipboard. xclip covers X11, wl-clipboard covers Wayland (the GNOME default
# since Ubuntu 21.04). Neovim's clipboard=unnamedplus silently stops reaching
# the system clipboard without the one matching your session type.
#
# xsel is deliberately absent: it does the same job as xclip, and .zshrc only
# ever calls xclip.
wl-clipboard

# Modern CLI tools
bat
fd-find
fzf
git
git-lfs
imagemagick
jq
ripgrep
shellcheck
tmux
zsh

# Desktop integration (GNOME)
ffmpeg
gh
gnome-tweaks

# Security and system safety
fail2ban
timeshift
ufw

# Agentic engineering productivity
direnv    # per-directory env vars -- agents cd in and env is set automatically
just      # task runner -- agents call `just <task>` without parsing Makefiles
shfmt     # shell script formatter -- agents write shell; keeps it well-formatted

# Shell and benchmarking -- the cross-platform half of the macOS Brewfile
zoxide     # cd replacement, wired over `cd` itself in .zshrc
hyperfine  # benchmarking with warmup runs and outlier detection; --verify uses it
